---
import ServiceDetailPage from '../../../components/ServiceDetailPage.astro';
import { getEntry } from 'astro:content';

const page = await getEntry('pages', 'service-corporate-teambuilding');
const { title, description, summary, bullets, audience, ctaLabel } = page.data;
---

<ServiceDetailPage
  title="Corporate Teambuilding"
  {description}
  {summary}
  {bullets}
  {audience}
  {ctaLabel}
/>
